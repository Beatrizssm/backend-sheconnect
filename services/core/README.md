# Core microservice

Build context: repository root (`services/Dockerfile` with `SERVICE_ENTRY=microservices/main-core`).

Port: **3000**

Routes: business domains, WebSocket `/socket.io`, Swagger `/api/docs`.
